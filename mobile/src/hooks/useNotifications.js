// PUSH NOTIFICATIONS DISABLED
// This service is currently disabled to ensure maximum stability on Expo Go (SDK 53+).
export const useNotifications = () => {
  // Currently a no-op to prevent mobile crashes
  return null;
};
